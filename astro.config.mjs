// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  base: '/', // Use the root path since your custom domain points to the site's root.
  // Other configuration options can go here
});


